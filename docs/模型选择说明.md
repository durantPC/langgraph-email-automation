# 🎯 模型选择说明

> 📋 **文档导航**: [返回文档索引](README.md) | [硅基流动配置](硅基流动配置指南.md)

## ⚠️ 重要提示

你提到想使用 **Qwen3-VL-32B-Instruct** 模型，但需要注意：

### 模型名称确认

在硅基流动平台上，模型名称必须**严格匹配**。常见的Qwen模型命名格式：

1. **Qwen2系列**（当前主流）：
   - `Qwen/Qwen2-VL-7B-Instruct`
   - `Qwen/Qwen2-VL-32B-Instruct`  ← 可能是这个
   - `Qwen/Qwen2-72B-Instruct`

2. **Qwen2.5系列**（最新）：
   - `Qwen/Qwen2.5-7B-Instruct`
   - `Qwen/Qwen2.5-72B-Instruct`

3. **Qwen3系列**（如果存在）：
   - `Qwen/Qwen3-VL-32B-Instruct`  ← 你提到的

---

## 🔍 如何确认模型名称

### 方法1：访问硅基流动控制台
1. 登录 https://cloud.siliconflow.cn/
2. 进入"模型市场"或"模型列表"
3. 搜索 "Qwen" 或 "32B"
4. 复制准确的模型名称

### 方法2：查看官方文档
访问：https://docs.siliconflow.cn/docs/model-list

---

## 🔧 修改模型名称

### 需要修改的文件

#### 1. `src/agents.py`（第14行）

将：
```python
qwen_llm = ChatOpenAI(
    model="Qwen/Qwen2-VL-7B-Instruct",  # ← 这里
    temperature=0.1,
    openai_api_key=os.getenv("SILICONFLOW_API_KEY"),
    openai_api_base="https://api.siliconflow.cn/v1"
)
```

改为你确认的模型名称：
```python
qwen_llm = ChatOpenAI(
    model="Qwen/Qwen3-VL-32B-Instruct",  # ← 你想用的模型
    temperature=0.1,
    openai_api_key=os.getenv("SILICONFLOW_API_KEY"),
    openai_api_base="https://api.siliconflow.cn/v1"
)
```

#### 2. `create_index.py`（第47行）

将：
```python
llm = ChatOpenAI(
    model="Qwen/Qwen2-VL-7B-Instruct",  # ← 这里
    temperature=0.1,
    openai_api_key=os.getenv("SILICONFLOW_API_KEY"),
    openai_api_base="https://api.siliconflow.cn/v1"
)
```

改为：
```python
llm = ChatOpenAI(
    model="Qwen/Qwen3-VL-32B-Instruct",  # ← 同样改这里
    temperature=0.1,
    openai_api_key=os.getenv("SILICONFLOW_API_KEY"),
    openai_api_base="https://api.siliconflow.cn/v1"
)
```

---

## 📝 快速修改指南

### 如果你确认模型名称是 `Qwen/Qwen3-VL-32B-Instruct`

直接编辑这两个文件，将所有的：
```
Qwen/Qwen2-VL-7B-Instruct
```

替换为：
```
Qwen/Qwen3-VL-32B-Instruct
```

---

## 💡 推荐的做法

### 步骤1：先测试当前配置
```bash
python create_index.py
```

如果成功，说明当前的 `Qwen2-VL-7B-Instruct` 模型可用。

### 步骤2：确认目标模型
访问硅基流动控制台，确认 `Qwen3-VL-32B-Instruct` 是否存在。

### 步骤3：修改并测试
修改模型名称后，重新运行：
```bash
python create_index.py
```

如果报错"模型不存在"，说明名称不对，需要调整。

---

## 🎯 可能的模型名称

根据硅基流动的命名规则，可能是以下之一：

| 可能的名称 | 说明 |
|-----------|------|
| `Qwen/Qwen3-VL-32B-Instruct` | Qwen3系列，32B参数 |
| `Qwen/Qwen2-VL-32B-Instruct` | Qwen2系列，32B参数 |
| `Qwen/Qwen2.5-VL-32B-Instruct` | Qwen2.5系列，32B参数 |
| `Qwen/QwenVL-32B-Instruct` | 简化命名 |

**建议：** 先在硅基流动控制台确认准确的模型名称！

---

## ⚠️ 注意事项

### 1. 模型成本
32B参数的模型比7B模型：
- ⚠️ **价格更高**（可能是7B的4-5倍）
- ⚠️ **速度更慢**（响应时间更长）
- ✅ **效果更好**（理解能力更强）

### 2. API配额
- 确保账户有足够的余额或免费额度
- 32B模型可能消耗配额更快

### 3. 超时设置
如果使用32B模型，可能需要增加超时时间：

```python
qwen_llm = ChatOpenAI(
    model="Qwen/Qwen3-VL-32B-Instruct",
    temperature=0.1,
    request_timeout=60,  # 增加超时时间到60秒
    openai_api_key=os.getenv("SILICONFLOW_API_KEY"),
    openai_api_base="https://api.siliconflow.cn/v1"
)
```

---

## 🚀 一键修改脚本

如果你确认模型名称，可以用我帮你切换。告诉我确认的模型名称，我会帮你修改代码。

---

## 📞 需要帮助？

如果不确定模型名称：
1. 先访问硅基流动控制台确认
2. 或者先用默认的 `Qwen2-VL-7B-Instruct` 测试
3. 确认模型名称后再切换

---

**总结：** 我已经将代码配置为使用硅基流动API，当前使用的是 `Qwen2-VL-7B-Instruct` 模型。如果你确认了 `Qwen3-VL-32B-Instruct` 在硅基流动上可用，只需修改两处模型名称即可！

